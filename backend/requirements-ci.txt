# requirements-ci.txt
# ---------------------------------------------------------------------------
# Lightweight dependency set for CI / GitHub Actions.
#
# Excludes heavy ML frameworks (PaddleOCR, PaddlePaddle, Ultralytics/YOLO),
# platform-specific packages (pywin32), and system-dependent libraries
# (pdf2image → poppler, nvidia-ml-py → NVIDIA driver).
#
# These services run as separate Docker containers in production; the core
# FastAPI app and its test suite do not import them directly.
# ---------------------------------------------------------------------------

# FastAPI 框架
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# 数据验证
pydantic>=2.10.0
pydantic-settings>=2.7.0

# 文件处理（lightweight subset）
python-docx==1.2.0
PyMuPDF==1.27.2.2
Pillow>=11.0.0
# pdf2image — excluded: requires system poppler
# pywin32 — excluded: Windows-only

# HTTP 客户端
httpx>=0.28.0

# nvidia-ml-py — excluded: requires NVIDIA drivers
aiofiles>=24.1.0

# 智谱AI SDK
zhipuai>=2.0.0

# ultralytics — excluded: heavy ML, requires torch
# paddleocr — excluded: heavy ML, requires paddlepaddle

# 工具库
PyJWT>=2.8.0
passlib[bcrypt]==1.7.4

# 可观测性
python-json-logger>=2.0.0
prometheus-client>=0.20.0

# 安全
cryptography>=42.0.0

# 开发工具
python-dotenv>=1.0.1
pytest>=8.0.0
