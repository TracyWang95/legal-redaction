# FastAPI framework
fastapi>=0.136.1
uvicorn[standard]>=0.32.0
python-multipart>=0.0.26

# Data validation
pydantic>=2.10.0
pydantic-settings>=2.7.0

# File processing
python-docx==1.2.0
PyMuPDF==1.27.2.2
Pillow>=12.2.0
pdf2image==1.17.0
pywin32==311; sys_platform == 'win32'  # Windows: .doc processing

# HTTP client
httpx>=0.28.0

# GPU memory checks for /health/services through NVML.
nvidia-ml-py>=12.535.133
aiofiles>=24.1.0

# Local app/model environment. PaddlePaddle GPU is installed explicitly by
# `npm run setup` from the official CUDA wheel index, and is repeated here so
# `pip install -r backend/requirements.txt` keeps the app venv complete.
# vLLM intentionally lives in .venv-vllm because its Torch/CUDA stack conflicts
# with Paddle/PaddleX when installed into the same environment.
--extra-index-url https://www.paddlepaddle.org.cn/packages/stable/cu129/
paddlepaddle-gpu==3.3.0
paddleocr[doc-parser]==3.5.0
llama-cpp-python[server]>=0.3.16

# Utility libraries
PyJWT>=2.12.0
passlib[bcrypt]==1.7.4

# Observability
python-json-logger>=2.0.0
prometheus-client>=0.20.0

# Security
cryptography>=46.0.7
# pyclamd>=0.4.0  # Optional; requires a ClamAV daemon.

# Development tools
python-dotenv>=1.2.2
pytest>=9.0.3
setuptools>=77,<81
