# Pinned dependency versions for DataInfra-RedactionEverything backend.
# Keep in sync with requirements.txt.  Re-generate after any dependency change
# by running:  pip freeze -r requirements.txt > requirements.lock
#
# Last updated: 2026-04-09

# FastAPI 框架
fastapi==0.117.1
uvicorn[standard]==0.35.0
python-multipart==0.0.20

# 数据验证
pydantic==2.12.5
pydantic-settings==2.13.1

# 文件处理
python-docx==1.2.0
PyMuPDF==1.27.2.2
Pillow==11.1.0
pdf2image==1.17.0
pywin32==311 ; sys_platform == 'win32'

# HTTP 客户端
httpx==0.28.1

# GPU 显存
nvidia-ml-py==12.535.133
aiofiles==24.1.0

# HaS Image / OCR — 已拆分为独立微服务（Dockerfile.ocr / Dockerfile.vision）
# 主后端不再直接依赖 ultralytics / paddleocr

# 工具库
PyJWT==2.11.0
passlib[bcrypt]==1.7.4

# 可观测性
python-json-logger==2.0.4
prometheus-client==0.21.1

# 安全
cryptography==46.0.1

# 运行时工具
python-dotenv==1.1.1
